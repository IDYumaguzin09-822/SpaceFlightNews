<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/news_item_linear_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:clickable="true"
    android:focusable="true"
    android:orientation="vertical">

    <TextView
        android:id="@+id/news_published_date"
        android:text="2020-12-18T11:44:43.000Z"
        style="@style/text_style" />

    <ImageView
        android:id="@+id/news_image"
        android:layout_width="match_parent"
        android:layout_height="230dp"
        android:background="@color/white"
        tools:srcCompat="@tools:sample/backgrounds/scenic" />

    <TextView
        android:id="@+id/news_title"
        style="@style/text_style_parent"
        android:text="Investigation confirms improperly connected cables caused Vega launch failure" />

    <TextView
        android:id="@+id/news_site"
        android:paddingRight="12dp"
        android:text="SpaceNews"
        android:textAlignment="textEnd"
        style="@style/text_style_parent" />
</LinearLayout>